import { RecyclerExample } from './examples'
import StoryModle from './StoryModle'

const { Template, story } = StoryModle(RecyclerExample)
export default story
export const Sample = Template.bind({})
